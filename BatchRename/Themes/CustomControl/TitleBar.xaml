<UserControl
    x:Class="BatchRename.Themes.CustomControl.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ct="clr-namespace:BatchRename.Themes.CustomControl"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <Border
        DockPanel.Dock="Top"
        Height="{Binding Height, RelativeSource={RelativeSource AncestorType={x:Type ct:TitleBar}}}"
        x:Name="titleBar"
        Padding="{DynamicResource PaddingLeftMedium}"
        Background="{DynamicResource BackgroundColor}">
        <Grid>
            <!--  Logo & Save State  -->
            <ct:LogoWithSaveState SaveState="Saved" />

            <!--  Menu  -->
            <ct:TopMenu x:Name="topMenu" />

            <!--  Window Button Command  -->
            <StackPanel
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Orientation="Horizontal"
                Height="40">

                <ct:WindowCommandButtons />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>