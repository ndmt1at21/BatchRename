<UserControl x:Class="ReplaceCharacter.ReplaceCharacterComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReplaceCharacter"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="340">
    <Grid Background="Wheat">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text ="H1: Replace Character in File name" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"></TextBlock>
        <TextBlock Text="Character: " Grid.Column="0" Grid.Row="1"></TextBlock>
        <TextBox Text="{Binding OldChacter, Mode=TwoWay}" Grid.Column="1" Grid.Row="1"></TextBox>
        <TextBlock Text="Replace with: " Grid.Column="0" Grid.Row="2"></TextBlock>
        <TextBox Text="{Binding OldChacter, Mode=TwoWay}" Grid.Column="1" Grid.Row="2"></TextBox>
        
    </Grid>
</UserControl>
